:root {
  --primary-black: #4d1018;
  --secondary-black: #6c1823;
  --surface-black: rgba(77, 16, 24, 0.72);
  --border-black: rgba(77, 16, 24, 0.18);
  --gold: #feb511;
  --gold-light: #fff1b8;
  --gold-dark: #c48d0c;
  --cream: #fef8e4;
  --white: #fcfaf6;
  --ink-muted: #8c5a14;
  --ink-soft: rgba(77, 16, 24, 0.72);
  --success: #2b8a5e;
  --error: #c2253b;
  --shadow-xl: 0 30px 80px rgba(77, 16, 24, 0.22);
  --shadow-lg: 0 20px 50px rgba(77, 16, 24, 0.16);
  --shadow-md: 0 12px 30px rgba(77, 16, 24, 0.1);
  --shadow-sm: 0 4px 12px rgba(77, 16, 24, 0.06);
  --radius-xl: 2rem;
  --radius-lg: 1.5rem;
  --radius-md: 1rem;
  --radius-sm: 0.75rem;
  --radius-xs: 0.5rem;
  --transition-base: 220ms ease;
  --transition-smooth: 400ms cubic-bezier(0.22, 1, 0.36, 1);
  --sidebar-width: 260px;
  --sidebar-collapsed-width: 72px;
  --header-height: 64px;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
  overflow-x: hidden;
  width: 100%;
}

body {
  font-family:
    "Inter",
    -apple-system,
    BlinkMacSystemFont,
    "Segoe UI",
    Roboto,
    sans-serif;
  background:
    radial-gradient(ellipse at 30% 0%, rgba(255, 241, 184, 0.98), rgba(254, 181, 17, 0.62) 40%, rgba(77, 16, 24, 0.08) 100%);
  color: var(--primary-black);
  overflow-x: hidden;
  width: 100%;
  min-height: 100vh;
  text-rendering: optimizeLegibility;
  -webkit-font-smoothing: antialiased;
  line-height: 1.6;
}

body::before {
  content: "";
  position: fixed;
  inset: 0;
  pointer-events: none;
  background-image:
    linear-gradient(rgba(77, 16, 24, 0.06) 1px, transparent 1px),
    linear-gradient(90deg, rgba(77, 16, 24, 0.06) 1px, transparent 1px);
  background-size: 80px 80px;
  mask-image: linear-gradient(to bottom, transparent, rgba(77, 16, 24, 0.15) 20%, transparent 85%);
  opacity: 0.3;
  z-index: -1;
}

body::after {
  content: "";
  position: fixed;
  inset: auto 0 -20vh 0;
  height: 45vh;
  pointer-events: none;
  background: radial-gradient(circle at center, rgba(77, 16, 24, 0.12), transparent 70%);
  filter: blur(60px);
  z-index: -1;
}

h1,
h2,
h3,
h4,
h5,
h6,
.display-1,
.display-2,
.display-3,
.display-4,
.display-5 {
  font-family:
    "Playfair Display",
    Georgia,
    "Times New Roman",
    serif;
  font-weight: 600;
  letter-spacing: 0.02em;
}

.app-shell {
  position: relative;
  isolation: isolate;
  display: flex;
  min-height: 100vh;
}

.admin-sidebar {
  position: fixed;
  top: 0;
  left: 0;
  height: 100vh;
  width: var(--sidebar-width);
  background: rgba(255, 248, 228, 0.96);
  border-right: 1px solid rgba(77, 16, 24, 0.08);
  backdrop-filter: blur(20px);
  z-index: 1000;
  display: flex;
  flex-direction: column;
  transition: width var(--transition-smooth), transform var(--transition-smooth);
  overflow-y: auto;
}

.admin-sidebar.collapsed {
  width: var(--sidebar-collapsed-width);
}

.admin-sidebar-header {
  padding: 1.25rem 1rem;
  border-bottom: 1px solid rgba(77, 16, 24, 0.08);
  display: flex;
  align-items: center;
  justify-content: space-between;
  min-height: var(--header-height);
}

.admin-sidebar-brand {
  text-decoration: none;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  color: var(--primary-black);
  transition: opacity var(--transition-base);
}

.admin-sidebar.collapsed .admin-sidebar-brand span {
  display: none;
}

.brand-text {
  font-family: "Playfair Display", Georgia, serif;
  font-size: 1.15rem;
  font-weight: 700;
  letter-spacing: 0.18em;
  color: var(--primary-black);
  white-space: nowrap;
  overflow: hidden;
}

.sidebar-toggle {
  width: 36px;
  height: 36px;
  border: 1px solid rgba(77, 16, 24, 0.15);
  border-radius: var(--radius-sm);
  background: transparent;
  cursor: pointer;
  display: flex;
  align-items: center;
  justify-content: center;
  color: var(--primary-black);
  transition: border-color var(--transition-base), background var(--transition-base);
}

.sidebar-toggle:hover {
  border-color: rgba(77, 16, 24, 0.3);
  background: rgba(77, 16, 24, 0.04);
}

.admin-sidebar-nav {
  flex: 1;
  padding: 1rem;
  overflow-y: auto;
}

.nav-section {
  margin-bottom: 1.5rem;
}

.nav-section-label {
  font-size: 0.65rem;
  letter-spacing: 0.2em;
  text-transform: uppercase;
  color: var(--ink-muted);
  padding: 0 0.75rem;
  margin-bottom: 0.5rem;
  white-space: nowrap;
  opacity: 0;
  transition: opacity var(--transition-base);
}

.admin-sidebar:not(.collapsed) .nav-section-label {
  opacity: 1;
}

.nav-link-item {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.7rem 0.85rem;
  border-radius: var(--radius-md);
  color: var(--primary-black);
  text-decoration: none;
  font-size: 0.85rem;
  font-weight: 500;
  letter-spacing: 0.04em;
  opacity: 0.7;
  transition: all var(--transition-base);
  white-space: nowrap;
  border: none;
  background: none;
  width: 100%;
  text-align: left;
  cursor: pointer;
}

.nav-link-item:hover,
.nav-link-item.router-link-active {
  background: rgba(77, 16, 24, 0.06);
  opacity: 1;
}

.nav-link-item.router-link-active {
  background: linear-gradient(90deg, rgba(77, 16, 24, 0.1), transparent);
  border-left: 3px solid var(--primary-black);
}

.nav-link-item i {
  font-size: 1.15rem;
  width: 1.5rem;
  text-align: center;
  flex-shrink: 0;
}

.admin-sidebar.collapsed .nav-link-item span {
  display: none;
}

.admin-sidebar.collapsed .nav-link-item {
  justify-content: center;
  padding: 0.7rem;
}

.admin-sidebar-footer {
  padding: 1rem;
  border-top: 1px solid rgba(77, 16, 24, 0.08);
}

.admin-user-info {
  display: flex;
  align-items: center;
  gap: 0.75rem;
  padding: 0.5rem;
  border-radius: var(--radius-md);
  background: rgba(255, 241, 184, 0.4);
}

.admin-sidebar.collapsed .admin-user-info span {
  display: none;
}

.admin-user-avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--primary-black), var(--secondary-black));
  color: var(--gold);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 600;
  font-size: 0.9rem;
  flex-shrink: 0;
}

.admin-user-details {
  min-width: 0;
  overflow: hidden;
}

.admin-user-name {
  font-weight: 600;
  font-size: 0.85rem;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}

.admin-user-role {
  font-size: 0.7rem;
  text-transform: uppercase;
  letter-spacing: 0.1em;
  color: var(--ink-muted);
  opacity: 0.8;
}
